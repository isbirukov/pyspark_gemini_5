1. В чем разница между rank() и dense_rank()?

2. Зачем нужно указывать rowsBetween? 
(Ответ: Чтобы ограничить диапазон агрегации, например, для скользящего среднего за последние 3 дня).

3. Какая функция позволяет получить значение из следующей строки?
(Ответ: lead).

4. Почему опасно использовать окно без partitionBy на больших данных? 
(Ответ: Все данные уходят на один узел для глобальной сортировки).
