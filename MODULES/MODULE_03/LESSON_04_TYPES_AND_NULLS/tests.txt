1. Что вернет выражение F.lit(10) + F.lit(None)?
(Ответ: Null).

2. Как проверить колонку на пустоту в filter?
(Ответ: .isNull()).

3. В чем разница между df.na.fill(0) и F.coalesce(col, F.lit(0))?
(Ответ: na.fill работает на весь DF или список колонок, coalesce — внутри выражения для конкретной колонки).

4. Если сделать .cast("int") для строки "abc", что произойдет?
(Ответ: вернется Null, ошибка не упадет).
